apiVersion: v1
kind: ConfigMap

metadata:
  name: ride-connect-configmap
  namespace: ride-connect

data:
  SERVER_PORT: "8080"

  SPRING_DATASOURCE_URL: jdbc:mysql://192.168.0.107:3306/ride_connect_db

  SPRING_JPA_SHOW_SQL: "true"

  SPRING_JPA_HIBERNATE_DDL_AUTO: update

  SPRING_DATA_WEB_PAGEABLE_SERIALIZATION_MODE: via-dto